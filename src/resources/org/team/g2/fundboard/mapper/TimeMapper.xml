<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.team.g2.fundboard.mapper.TimeMapper">

    <select id="getTime" resultMap="string">
        select now()
    </select>
</mapper>